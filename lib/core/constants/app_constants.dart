class AppConstants {
  AppConstants._();

  static const String appName = 'Academix';
  static const int pageSize = 20;
  static const Duration networkTimeout = Duration(seconds: 30);
  static const String defaultDateFormat = 'dd MMM yyyy';
}
